#ifndef MAPINFORMATION
#define MAPINFORMATION
#include "DrawableObject.h"
#include "ObjectGenerator.h"

class MapInformation {
private:

public:
	MapInformation();

	void draw(float i_frameTime);
	void update(float i_frameTime);
};

#endif#pragma once
